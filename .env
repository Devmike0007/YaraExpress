# Development environment configuration
DEBUG=True
SECRET_KEY=django-insecure-dev-key-change-this
ALLOWED_HOSTS=localhost,127.0.0.1
WHATSAPP_NUMBER=243858983606

# For Render deployment, add your domain:
# ALLOWED_HOSTS=localhost,127.0.0.1,your-app.onrender.com

# Database URL (set by Render automatically when PostgreSQL is added)
# DATABASE_URL will be automatically set by Render when you add a PostgreSQL instance
